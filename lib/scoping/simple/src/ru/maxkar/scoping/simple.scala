package ru.maxkar.scoping

/** Simple scoping library. Each scope should define
 * no more than one object with a given key.
 * However, effective set of visible names may contain
 * several instances of the name if that names are "imported"
 * from different active scopes.
 */
package object simple {
}
